# .github/workflows/main.yml

name: Main Workflow

on: [push]

jobs:
  call-workflow-2-in-local-repo:
    uses: ./.github/workflows/deploy.yml
    with:
      tfVersion: 1.3.1
  call-workflow-2-in-other-repo:
    uses: SuprajMaripeddi/terraform-github-actions/.github/workflows/deploy.yml@AWS
    with:
      environment: prodcusfisoadss
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
#       - name: Deploy code
#         uses: ./.github/workflows/deploy.yml
#         with:
#           environment: production
